# ai_group_ui

基于 Vue 3 + Element Plus 的 AI 群聊前端。

## 启动

```bash
npm install
npm run dev
```

默认后端地址：`http://localhost:8000/api/v1`

可通过环境变量覆盖：

```bash
echo "VITE_API_BASE=http://localhost:8000/api/v1" > .env
```
